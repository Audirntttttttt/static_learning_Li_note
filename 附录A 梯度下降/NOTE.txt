梯度下降法的原理及其R语言实现

梯度下降法（gradient decent）是求解无约束最优化问题的最常见的方法。

对于梯度，我们可以这样理解，负梯度是函数下降最快的方向，也就是说，函数在一点上的负梯度，是一个向量，指向函数的最低点。但是，如果换成多元函数，上面的“指向函数最低点”，实际是错误的。实际上应该是，函数在梯度方向上的最低点，因此，我们在一个方向达到最低点是，应当观察是否可以继续向其他方向下降。

输入：目标函数（凸函数），阈值
输出：函数的极值点